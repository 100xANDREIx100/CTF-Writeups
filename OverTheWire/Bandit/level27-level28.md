# OverTheWire - Bandit - Level 27 -> Level 28

## 🎯 Objective
Use `git` to download a repo

## 🛠️ Tools & Concepts Used
* `git` - distributed control system

## 📝 Step-by-Step Solution

**1. Copy the repo**
For this level I just had to copy the repo and `cat` the `README` file

```bash
git clone ssh://bandit27-git@bandit.labs.overthewire.org:2220/home/bandit27-git/repo
```
It directly outputted the password for the next level

## 💡 Key Takeaway
Refreshed `git` use
